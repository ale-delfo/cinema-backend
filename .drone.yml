kind: pipeline
type: exec
name: backend-builder

platform:
  os: linux
  arch: amd64

steps:
  - name: building-image
    commands:
      - docker build -t backend:0.0.1 .

trigger:
  branch: 
    - master
  event:
    - tag